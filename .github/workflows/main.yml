on: [push]

jobs:
  Pdf_job:
    runs-on: ubuntu-latest
    name: Checking pdf
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v2
      - name: Building and running Dockerfile
        uses: ./ # Uses an action in the root directory

